apiVersion: v1
kind: Service
metadata:
  name: rag-app
  labels:
    app: rag-app
spec:
  selector:
    app: rag-app
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
