apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rag-app
  labels:
    app: rag-app
spec:
  to:
    kind: Service
    name: rag-app
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
